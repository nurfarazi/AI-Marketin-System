# AI Marketing System Web

React + TypeScript dashboard for the AI Marketing System API.

## Run

```bash
cd web
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `web/.env` if your API is not running on `http://localhost:5011`.
